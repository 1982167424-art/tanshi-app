# 2026-07-20 工作日志

## 验证页跳过逻辑改造（移除30秒等待）
- 需求：Turnstile 脚本加载失败/转圈/widget 渲染超时等所有等待状态都直接显示跳过按钮，不再等 30 秒
- 改动文件：src/pages/Verify.tsx
  - 删除 SKIP_TIMEOUT_MS（30秒）常量和 skipTimerRef
  - 删除 showSkip state，改为 canSkip = status !== 'success' && !skipConfirming
  - loading/loadFailed/verifying/error 四种状态均直接显示"跳过验证"入口
  - 页面加载时预先检查冷却期（原来在 30s timer 回调里检查）
  - 按钮文案"继续访问"→"跳过验证"，增加 active:bg-amber-700 移动端反馈
  - 保留 5 秒确认倒计时 + 1 小时冷却安全策略
- 部署：commit 934d7d2，prebuilt 部署成功（index-D8guEct1.js）
- **Vercel CLI 代理问题**：Node.js 22 的 undici 默认不读 HTTP_PROXY/HTTPS_PROXY，需加 `NODE_USE_ENV_PROXY=1` 环境变量才能让 Vercel CLI 走代理。完整命令：`NODE_USE_ENV_PROXY=1 HTTPS_PROXY=http://127.0.0.1:1082 npx vercel deploy --prod --prebuilt --yes`
